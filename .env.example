# VoiceShield — Environment Configuration
# Copy this file to .env and adjust paths for your system.
# All variables are optional — defaults work if data/ folder exists.

# Dataset directory (contains metadata_clean.csv + train/val/test folders)
# VOICESHIELD_DATASET=./data/dataset

# Trained model output directory
# VOICESHIELD_MODELS=./data/models

# TensorBoard log directory
# VOICESHIELD_LOGS=./data/logs

# Direct path to a specific .keras model (overrides auto-discovery)
# VOICESHIELD_MODEL_PATH=./data/models/resnet_v2_run1/best_model.keras

# Upload directory for API temp files
# VOICESHIELD_UPLOADS=./data/uploads
